**Scope**

* Wire CreateContractSheet to the documented Contract API for creating contracts and fetching metadata.

* Keep layout intact; enhance UX with validated submits, loading/disabled states, and toasts.

* Use existing axios helpers and React Query patterns per project rules.

**Data Fetching**

* Add React Query calls in CreateContractSheet to populate selects:

  * GET "/types"

